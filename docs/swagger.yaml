basePath: /api/v1
definitions:
  apperrors.ErrorResponse:
    properties:
      error:
        type: string
    type: object
  apperrors.SuccessResponse:
    properties:
      message:
        type: string
    type: object
  model.Bus:
    properties:
      brand:
        type: string
      capacity:
        type: integer
      category:
        $ref: '#/definitions/model.BusCategory'
      category_id:
        type: integer
      created_at:
        type: string
      deleted_at:
        type: string
      id:
        type: integer
      license_plate:
        type: string
      status:
        $ref: '#/definitions/model.BusStatus'
      status_id:
        type: integer
      updated_at:
        type: string
    type: object
  model.BusCategory:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  model.BusStatus:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  model.BusUpdate:
    properties:
      brand:
        type: string
      capacity:
        type: integer
      category_id:
        type: integer
      id:
        type: integer
      license_plate:
        type: string
      status_id:
        type: integer
    type: object
  model.Driver:
    properties:
      first_name:
        type: string
      id:
        type: integer
      last_name:
        type: string
      middle_name:
        type: string
      phone_number:
        type: string
      status:
        $ref: '#/definitions/model.DriverStatus'
    type: object
  model.DriverInput:
    properties:
      first_name:
        type: string
      last_name:
        type: string
      middle_name:
        type: string
      phone_number:
        type: string
      status:
        type: integer
    type: object
  model.DriverStatus:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  model.TokenResponse:
    properties:
      access_token:
        type: string
    type: object
  model.UserCreate:
    properties:
      email:
        type: string
      name:
        type: string
      password:
        minLength: 8
        type: string
    required:
    - email
    - name
    - password
    type: object
  model.UserLogin:
    properties:
      email:
        type: string
      password:
        type: string
    required:
    - email
    - password
    type: object
  model.UserResponse:
    properties:
      created_at:
        type: string
      email:
        type: string
      id:
        type: integer
      name:
        type: string
      role:
        type: string
      updated_at:
        type: string
    type: object
  model.UserUpdate:
    properties:
      email:
        type: string
      name:
        type: string
      password:
        type: string
    type: object
host: localhost:8080
info:
  contact:
    email: luchitstimofei@yandex.ru
    name: Luchits Timofei
    url: http://example.com
  description: This is a server for order private bus.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: Ordering Bus API
  version: "1.0"
paths:
  /admin/bus:
    post:
      consumes:
      - application/json
      description: Создает новую запись об автобусе (требуются права администратора)
      parameters:
      - description: Данные автобуса
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.Bus'
      produces:
      - application/json
      responses:
        "200":
          description: Автобус успешно создан
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Создать новый автобус (только админ)
      tags:
      - admin/bus
  /admin/bus/{id}:
    delete:
      description: Удаляет запись об автобусе по ID (требуются права администратора)
      parameters:
      - description: ID автобуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Автобус успешно удален
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Автобус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Удалить автобус (только админ)
      tags:
      - admin/bus
    put:
      consumes:
      - application/json
      description: Обновляет информацию об автобусе по ID (требуются права администратора)
      parameters:
      - description: ID автобуса
        in: path
        name: id
        required: true
        type: integer
      - description: Обновленные данные автобуса
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.BusUpdate'
      produces:
      - application/json
      responses:
        "200":
          description: Данные автобуса обновлены
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Автобус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Обновить данные автобуса (только админ)
      tags:
      - admin/bus
  /admin/bus/categories:
    post:
      consumes:
      - application/json
      description: Создает новую категорию автобуса (только для администраторов)
      parameters:
      - description: Данные категории
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.BusCategory'
      produces:
      - application/json
      responses:
        "201":
          description: Категория успешно создана
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "409":
          description: Категория с таким названием уже существует
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Создать новую категорию автобуса
      tags:
      - admin/bus/categories
  /admin/bus/categories/{id}:
    delete:
      description: Удаляет категорию автобуса по ID (только для администраторов)
      parameters:
      - description: ID категории
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Категория успешно удалена
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Удалить категорию автобуса
      tags:
      - admin/bus/categories
    put:
      consumes:
      - application/json
      description: Обновляет информацию о категории автобуса по ID (только для администраторов)
      parameters:
      - description: ID категории
        in: path
        name: id
        required: true
        type: integer
      - description: Обновленные данные категории
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.BusCategory'
      produces:
      - application/json
      responses:
        "200":
          description: Обновленные данные категории
          schema:
            $ref: '#/definitions/model.BusCategory'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "409":
          description: Категория с таким названием уже существует
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Обновить категорию автобуса
      tags:
      - admin/bus/categories
  /admin/bus/statuses:
    post:
      consumes:
      - application/json
      description: Создает новую запись статуса автобуса. Доступно только администраторам
      parameters:
      - description: Данные статуса
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.BusStatus'
      produces:
      - application/json
      responses:
        "201":
          description: Статус успешно создан
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Создать новый статус автобуса
      tags:
      - admin/bus/statuses
  /admin/bus/statuses/{id}:
    delete:
      description: Удаляет статус автобуса по его идентификатору. Доступно только
        администраторам
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Статус успешно удален
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Статус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Удалить статус автобуса
      tags:
      - admin/bus/statuses
    put:
      consumes:
      - application/json
      description: Обновляет данные статуса автобуса по его идентификатору. Доступно
        только администраторам
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      - description: Обновленные данные статуса
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.BusStatus'
      produces:
      - application/json
      responses:
        "200":
          description: Обновленные данные статуса
          schema:
            $ref: '#/definitions/model.BusStatus'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен. Требуются права администратора
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Статус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Обновить статус автобуса
      tags:
      - admin/bus/statuses
  /admin/driver:
    post:
      consumes:
      - application/json
      description: Создает новую запись о водителе (требуются права администратора)
      parameters:
      - description: Данные водителя
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.DriverInput'
      produces:
      - application/json
      responses:
        "201":
          description: Водитель успешно создан
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Создать нового водителя (только админ)
      tags:
      - admin/driver
  /admin/driver/{id}:
    delete:
      description: Удаляет запись о водителе по ID (требуются права администратора)
      parameters:
      - description: ID водителя
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Водитель успешно удален
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Водитель не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Удалить водителя (только админ)
      tags:
      - admin/driver
    put:
      consumes:
      - application/json
      description: Обновляет информацию о водителе по ID (требуются права администратора)
      parameters:
      - description: ID водителя
        in: path
        name: id
        required: true
        type: integer
      - description: Обновленные данные водителя
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.DriverInput'
      produces:
      - application/json
      responses:
        "200":
          description: Данные водителя обновлены
          schema:
            $ref: '#/definitions/apperrors.SuccessResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Водитель не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Обновить данные водителя (только админ)
      tags:
      - admin/driver
  /admin/driver/status:
    post:
      description: Создание статуса водителя
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные статуса
          schema:
            $ref: '#/definitions/model.DriverStatus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Создать статус водителя
      tags:
      - admin/driver/status
  /admin/driver/status/{id}:
    delete:
      description: Удаление статуса водителя по ID
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Данные статуса
          schema:
            $ref: '#/definitions/model.DriverStatus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Удалить статус водителя
      tags:
      - admin/driver/status
    put:
      description: Изменение статуса по ID
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные статуса
          schema:
            $ref: '#/definitions/model.DriverStatus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Изменить статус по ID
      tags:
      - admin/driver/status
  /admin/users:
    post:
      consumes:
      - application/json
      description: Создает новую учетную запись пользователя (только для администраторов)
      parameters:
      - description: Данные пользователя
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.UserCreate'
      produces:
      - application/json
      responses:
        "201":
          description: Пользователь успешно создан
          schema:
            $ref: '#/definitions/model.UserResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "409":
          description: Пользователь с таким email уже существует
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Создать нового пользователя
      tags:
      - admin
  /admin/users/{id}:
    put:
      consumes:
      - application/json
      description: Обновляет информацию о пользователе по ID (только для администраторов)
      parameters:
      - description: ID пользователя
        in: path
        name: id
        required: true
        type: integer
      - description: Обновленные данные пользователя
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.UserUpdate'
      produces:
      - application/json
      responses:
        "200":
          description: Данные пользователя обновлены
          schema:
            $ref: '#/definitions/model.UserResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Пользователь не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Обновить данные пользователя
      tags:
      - admin
  /api/v1/buses/{id}:
    get:
      description: Возвращает информацию об автобусе по его идентификатору
      parameters:
      - description: ID автобуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные автобуса
          schema:
            $ref: '#/definitions/model.Bus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Автобус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить автобус по ID
      tags:
      - buses
  /auth/login:
    post:
      consumes:
      - application/json
      description: Вход пользователя в систему
      parameters:
      - description: Данные для входа
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.UserLogin'
      produces:
      - application/json
      responses:
        "200":
          description: Успешный вход
          schema:
            $ref: '#/definitions/model.TokenResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Неверные учетные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Аутентификация пользователя
      tags:
      - auth
  /auth/register:
    post:
      consumes:
      - application/json
      description: Создает нового пользователя в системе
      parameters:
      - description: Данные пользователя
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/model.UserCreate'
      produces:
      - application/json
      responses:
        "201":
          description: Успешная регистрация
          schema:
            $ref: '#/definitions/model.UserResponse'
        "400":
          description: Некорректные данные
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "409":
          description: Пользователь уже существует
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Регистрация нового пользователя
      tags:
      - auth
  /bus/categories:
    get:
      description: Возвращает список всех доступных категорий автобусов. Доступно
        всем пользователям
      produces:
      - application/json
      responses:
        "200":
          description: Список категорий автобусов
          schema:
            items:
              $ref: '#/definitions/model.BusCategory'
            type: array
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить все категории автобусов
      tags:
      - bus/categories
  /bus/categories/{id}:
    get:
      description: Возвращает информацию о статусе водителя по его идентификатору.
        Доступно всем пользователям
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные статуса
          schema:
            $ref: '#/definitions/model.DriverStatus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Категория не найдена
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить статус водителя по ID
      tags:
      - driver/status
  /bus/statuses:
    get:
      description: Возвращает список всех доступных статусов автобусов. Доступно всем
        пользователям
      produces:
      - application/json
      responses:
        "200":
          description: Список статусов автобусов
          schema:
            items:
              $ref: '#/definitions/model.BusStatus'
            type: array
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить все статусы автобусов
      tags:
      - bus/statuses
  /bus/statuses/{id}:
    get:
      description: Возвращает данные статуса автобуса по его идентификатору
      parameters:
      - description: ID статуса
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные статуса
          schema:
            $ref: '#/definitions/model.BusStatus'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Статус не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить статус автобуса по ID
      tags:
      - bus/statuses
  /buses:
    get:
      description: Возвращает список всех автобусов в системе
      produces:
      - application/json
      responses:
        "200":
          description: Список автобусов
          schema:
            items:
              $ref: '#/definitions/model.Bus'
            type: array
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить список всех автобусов
      tags:
      - buses
  /driver:
    get:
      description: Возвращает список всех водителей в системе
      produces:
      - application/json
      responses:
        "200":
          description: Список водителей
          schema:
            items:
              $ref: '#/definitions/model.Driver'
            type: array
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить список всех водителей
      tags:
      - drivers
  /driver/{id}:
    get:
      description: Возвращает информацию о водителе по его идентификатору
      parameters:
      - description: ID водителя
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные водителя
          schema:
            $ref: '#/definitions/model.Driver'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Водитель не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить водителя по ID
      tags:
      - drivers
  /driver/status:
    get:
      description: Возвращает список всех статусов водителя. Доступно всем пользователям
      produces:
      - application/json
      responses:
        "200":
          description: Список статусов водителя
          schema:
            items:
              $ref: '#/definitions/model.DriverStatus'
            type: array
        "404":
          description: Ничего не найдено
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      summary: Получить все статусы водителя
      tags:
      - driver/status
  /users:
    get:
      description: Возвращает список всех зарегистрированных пользователей (только
        для администраторов)
      produces:
      - application/json
      responses:
        "200":
          description: Список пользователей
          schema:
            items:
              $ref: '#/definitions/model.UserResponse'
            type: array
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Получить всех пользователей
      tags:
      - users
  /users/{id}:
    delete:
      description: Удаляет учетную запись пользователя по ID (только для администраторов)
      parameters:
      - description: ID пользователя
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Пользователь успешно удален
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Пользователь не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "500":
          description: Внутренняя ошибка сервера
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Удалить пользователя
      tags:
      - users
    get:
      description: Возвращает информацию о пользователе по его идентификатору
      parameters:
      - description: ID пользователя
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Данные пользователя
          schema:
            $ref: '#/definitions/model.UserResponse'
        "400":
          description: Некорректный ID
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "401":
          description: Не авторизован
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "403":
          description: Доступ запрещен
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
        "404":
          description: Пользователь не найден
          schema:
            $ref: '#/definitions/apperrors.ErrorResponse'
      security:
      - Bearer: []
      summary: Получить пользователя по ID
      tags:
      - users
securityDefinitions:
  Bearer:
    description: Type "Bearer" followed by a space and JWT token.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
